pyyaml>=6.0
requests>=2.31.0
yfinance>=0.2.0
anthropic>=0.40.0  # optional: role review falls back to the heuristic backend (HAS_ANTHROPIC)
beautifulsoup4>=4.12.0  # optional: only the public FinViz HTML path needs it (HAS_BS4)
